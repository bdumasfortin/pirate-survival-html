{
  "name": "pirate-survival-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "build:bundle": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/server.js && node --input-type=module -e \"import fs from 'node:fs'; fs.mkdirSync('dist', { recursive: true }); fs.copyFileSync('README_DIST.md', 'dist/README.md');\"",
    "start": "node dist/index.js",
    "start:bundle": "node dist/server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.21.5",
    "tsx": "^4.7.2",
    "typescript": "^4.9.5"
  }
}
